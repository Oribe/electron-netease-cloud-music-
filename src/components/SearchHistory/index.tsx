import React, { FC } from "react";
import styles from "./index.module.scss";

const SearchHistory: FC = () => {
  return <section className={styles.searchHistoryWrapper}>搜索历史</section>;
};

export default SearchHistory;
